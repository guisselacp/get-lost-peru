from django.db import models
from django.contrib.auth.models import User

# class Userprofile(models.Model):
#     author = models.ForeignKey(User, on_delete=models.CASCADE, related_name="userprofile")

    # class Meta:
    #     profile= 'author'
 


